{
  "name": "@teleblock/subgraph",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ teleblock",
    "create-local": "graph create --node http://localhost:8020/ teleblock",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 teleblock"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
