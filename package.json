{
  "name": "teleblock",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized, end-to-end-encrypted chat, groups & forums with a Telegram-grade UX",
  "license": "Apache-2.0",
  "workspaces": [
    "shared",
    "web",
    "indexer"
  ],
  "scripts": {
    "test:shared": "npm --workspace @teleblock/shared test",
    "test:indexer": "npm --workspace @teleblock/indexer test",
    "compile:contracts": "cd contracts && npm install && npm run compile",
    "test:contracts": "cd contracts && forge test -vvv",
    "test:web": "npm --workspace @teleblock/web run e2e",
    "test": "npm run test:shared && npm run test:indexer",
    "test:all": "npm run test:shared && npm run test:indexer && npm run test:web",
    "build:web": "npm --workspace @teleblock/web run build",
    "dev:web": "npm --workspace @teleblock/web run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
